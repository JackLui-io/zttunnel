# 开发环境：空字符串使用相对路径，Vite 代理会转发 HTTP 到后端 localhost:8026
VITE_API_BASE_URL=
# 车流 WebSocket 需直连后端（见 useTunnelWebSocket），勿用 5173；若后端端口非 8026 请改掉
# VITE_DEV_BACKEND_PORT=8026
# 或写完整地址（任意环境优先）：VITE_WS_URL=ws://127.0.0.1:8026/websocket/3
